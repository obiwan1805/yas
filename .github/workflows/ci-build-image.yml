name: "CI - Build Docker image by branch"

on:
  push:
    branches:
      - "**"
    paths:
      - "backoffice-bff/**"
      - "backoffice/**"
      - "cart/**"
      - "customer/**"
      - "delivery/**"
      - "inventory/**"
      - "location/**"
      - "media/**"
      - "order/**"
      - "payment/**"
      - "payment-paypal/**"
      - "product/**"
      - "promotion/**"
      - "rating/**"
      - "recommendation/**"
      - "sampledata/**"
      - "search/**"
      - "storefront-bff/**"
      - "storefront/**"
      - "tax/**"
      - "webhook/**"
      - "common-library/**"
      - "checkstyle/**"
      - "pom.xml"
      - ".github/workflows/actions/**"
      - ".github/workflows/ci-build-image.yml"

permissions:
  contents: write
  packages: write

env:
  GHCR_NAMESPACE: obiwan1805

jobs:
  build:
    name: Build ${{ matrix.service }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - service: backoffice-bff
            source_dir: backoffice-bff
            image_name: yas-backoffice-bff
            build_type: maven-f
            chart_dir: backoffice-bff

          - service: backoffice
            source_dir: backoffice
            image_name: yas-backoffice
            build_type: node
            chart_dir: backoffice-ui

          - service: cart
            source_dir: cart
            image_name: yas-cart
            build_type: maven-pl
            chart_dir: cart

          - service: customer
            source_dir: customer
            image_name: yas-customer
            build_type: maven-pl
            chart_dir: customer

          - service: delivery
            source_dir: delivery
            image_name: yas-delivery
            build_type: maven-pl
            chart_dir: delivery

          - service: inventory
            source_dir: inventory
            image_name: yas-inventory
            build_type: maven-pl
            chart_dir: inventory

          - service: location
            source_dir: location
            image_name: yas-location
            build_type: maven-pl
            chart_dir: location

          - service: media
            source_dir: media
            image_name: yas-media
            build_type: maven-pl
            chart_dir: media

          - service: order
            source_dir: order
            image_name: yas-order
            build_type: maven-pl
            chart_dir: order

          - service: payment
            source_dir: payment
            image_name: yas-payment
            build_type: maven-pl
            chart_dir: payment

          - service: payment-paypal
            source_dir: payment-paypal
            image_name: yas-payment-paypal
            build_type: maven-pl
            chart_dir: payment-paypal

          - service: product
            source_dir: product
            image_name: yas-product
            build_type: maven-pl
            chart_dir: product

          - service: promotion
            source_dir: promotion
            image_name: yas-promotion
            build_type: maven-pl
            chart_dir: promotion

          - service: rating
            source_dir: rating
            image_name: yas-rating
            build_type: maven-pl
            chart_dir: rating

          - service: recommendation
            source_dir: recommendation
            image_name: yas-recommendation
            build_type: maven-pl
            chart_dir: recommendation

          - service: sampledata
            source_dir: sampledata
            image_name: yas-sampledata
            build_type: maven-pl
            chart_dir: sampledata

          - service: search
            source_dir: search
            image_name: yas-search
            build_type: maven-pl
            chart_dir: search

          - service: storefront-bff
            source_dir: storefront-bff
            image_name: yas-storefront-bff
            build_type: maven-pl
            chart_dir: storefront-bff

          - service: storefront-ui
            source_dir: storefront
            image_name: yas-storefront
            build_type: node
            chart_dir: storefront-ui

          - service: tax
            source_dir: tax
            image_name: yas-tax
            build_type: maven-pl
            chart_dir: tax

          - service: webhook
            source_dir: webhook
            image_name: yas-webhook
            build_type: maven-pl
            chart_dir: webhook

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.MY_GITHUB_TOKEN }}

      - name: Decide whether to build this service
        id: decide
        shell: bash
        run: |
          set -euo pipefail

          SHOULD_BUILD="false"
          SHOULD_UPDATE_VALUES="false"

          BEFORE="${{ github.event.before }}"
          AFTER="${{ github.sha }}"

          if [ -z "$BEFORE" ] || echo "$BEFORE" | grep -q '^0000000000000000000000000000000000000000$'; then
            CHANGED_FILES="$(git diff-tree --no-commit-id --name-only -r "$AFTER" || true)"
          else
            CHANGED_FILES="$(git diff --name-only "$BEFORE" "$AFTER" || true)"
          fi

          echo "Changed files:"
          echo "$CHANGED_FILES"

          # Nếu đổi common/shared code hoặc workflow build thì build toàn bộ service.
          if echo "$CHANGED_FILES" | grep -Eq '^(common-library/|checkstyle/|pom\.xml|\.github/workflows/actions/|\.github/workflows/ci-build-image\.yml)'; then
            SHOULD_BUILD="true"
          fi

          # Nếu đổi trực tiếp folder service này thì build service này
          # và chỉ service này mới được update file k8s/charts/<chart_dir>/values-dev.yaml.
          if echo "$CHANGED_FILES" | grep -q "^${{ matrix.source_dir }}/"; then
            SHOULD_BUILD="true"
            SHOULD_UPDATE_VALUES="true"
          fi

          echo "should_build=$SHOULD_BUILD" >> "$GITHUB_OUTPUT"
          echo "should_update_values=$SHOULD_UPDATE_VALUES" >> "$GITHUB_OUTPUT"
          echo "Should build ${{ matrix.service }}: $SHOULD_BUILD"
          echo "Should update values for ${{ matrix.service }}: $SHOULD_UPDATE_VALUES"

      - name: Set up JDK 21
        if: steps.decide.outputs.should_build == 'true' && startsWith(matrix.build_type, 'maven')
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Set up Node.js 20
        if: steps.decide.outputs.should_build == 'true' && matrix.build_type == 'node'
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: "${{ matrix.source_dir }}/package-lock.json"

      - name: Build Maven module from root pom
        if: steps.decide.outputs.should_build == 'true' && matrix.build_type == 'maven-pl'
        shell: bash
        run: |
          mvn -B clean package -pl "${{ matrix.source_dir }}" -am -DskipTests

      - name: Build Maven standalone module
        if: steps.decide.outputs.should_build == 'true' && matrix.build_type == 'maven-f'
        shell: bash
        run: |
          mvn -B clean package -f "${{ matrix.source_dir }}" -DskipTests

      - name: Build Node service
        if: steps.decide.outputs.should_build == 'true' && matrix.build_type == 'node'
        shell: bash
        working-directory: ${{ matrix.source_dir }}
        run: |
          npm ci
          npm run build

      - name: Calculate Docker tags
        if: steps.decide.outputs.should_build == 'true'
        id: vars
        shell: bash
        run: |
          set -euo pipefail

          SHORT_SHA="$(git rev-parse --short=7 HEAD)"
          IMAGE="ghcr.io/${{ env.GHCR_NAMESPACE }}/${{ matrix.image_name }}"

          {
            echo "short_sha=$SHORT_SHA"
            echo "image=$IMAGE"
            echo "tags<<EOF"
            echo "$IMAGE:$SHORT_SHA"

            if [ "${{ github.ref }}" = "refs/heads/main" ]; then
              echo "$IMAGE:main"
            fi

            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          echo "Docker image tags:"
          echo "$IMAGE:$SHORT_SHA"

          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "$IMAGE:main"
          fi

      - name: Login to GHCR
        if: steps.decide.outputs.should_build == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        if: steps.decide.outputs.should_build == 'true'
        uses: docker/build-push-action@v6
        with:
          context: ./${{ matrix.source_dir }}
          push: true
          tags: ${{ steps.vars.outputs.tags }}

      - name: Update values-dev.yaml image tag
        if: steps.decide.outputs.should_build == 'true' && steps.decide.outputs.should_update_values == 'true' && github.ref == 'refs/heads/main'
        shell: bash
        run: |
          set -euo pipefail

          CHART_DIR="${{ matrix.chart_dir }}"
          VALUES_FILE="k8s/charts/${CHART_DIR}/values-dev.yaml"
          TAG="${{ steps.vars.outputs.short_sha }}"
          export TAG

          curl -fsSL https://github.com/mikefarah/yq/releases/download/v4.44.3/yq_linux_amd64 -o /tmp/yq
          chmod +x /tmp/yq

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          update_tag() {
            local file="$1"

            if [ "$(/tmp/yq e 'has("backend") and (.backend | has("image")) and (.backend.image | has("tag"))' "$file")" = "true" ]; then
              /tmp/yq -i '.backend.image.tag = strenv(TAG)' "$file"
              return 0
            fi

            if [ "$(/tmp/yq e 'has("ui") and (.ui | has("image")) and (.ui.image | has("tag"))' "$file")" = "true" ]; then
              /tmp/yq -i '.ui.image.tag = strenv(TAG)' "$file"
              return 0
            fi

            if [ "$(/tmp/yq e 'has("image") and (.image | has("tag"))' "$file")" = "true" ]; then
              /tmp/yq -i '.image.tag = strenv(TAG)' "$file"
              return 0
            fi

            echo "::error::Cannot find image tag path in $file. Expected backend.image.tag, ui.image.tag, or image.tag"
            return 1
          }

          for attempt in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main

            if [ ! -f "$VALUES_FILE" ]; then
              echo "::warning::Skip values update because $VALUES_FILE does not exist"
              exit 0
            fi

            update_tag "$VALUES_FILE"

            if git diff --quiet -- "$VALUES_FILE"; then
              echo "No values change for ${CHART_DIR}; tag is already ${TAG}"
              exit 0
            fi

            git add "$VALUES_FILE"
            git commit -m "chore(cd): update ${CHART_DIR} image tag to ${TAG} [skip ci]"

            if git push origin HEAD:main; then
              echo "Updated $VALUES_FILE => $TAG"
              exit 0
            fi

            echo "Push failed, retrying..."
            sleep $((attempt * 2))
          done

          echo "Failed to push values update after retries"
          exit 1

      - name: Print CI summary
        if: steps.decide.outputs.should_build == 'true'
        shell: bash
        run: |
          echo "## CI result for ${{ matrix.service }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          echo "**Service:** ${{ matrix.service }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          echo "**Docker image tags:**" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          echo "${{ steps.vars.outputs.tags }}" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

          echo "Built service: ${{ matrix.service }}"
          echo "Image tags:"
          echo "${{ steps.vars.outputs.tags }}"
